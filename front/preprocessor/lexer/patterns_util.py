import pydot
from graphlex import Relation

def add(g):
    return g


def join(g, node, pre=[]):
    return g


def fork(g, post=[], node=pydot.Node()):
    return g


def sequence(g, seq=[]):
    return g


def cycle(g, seq=[]):
    return g
